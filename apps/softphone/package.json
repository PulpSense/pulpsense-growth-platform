{
  "name": "@pulpsense/softphone",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "check-types": "tsc --noEmit",
    "dev": "vite",
    "preview": "wrangler pages dev dist",
    "deploy:production": "pnpm build && wrangler pages deploy dist --project-name pulpsense-softphone-preview"
  },
  "dependencies": {
    "@telnyx/webrtc": "2.27.9"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vite": "^8.0.0",
    "wrangler": "^4.120.0"
  }
}
